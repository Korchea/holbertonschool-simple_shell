#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[], char *envp[]) {
    while( *envp ) {
	    if (*envp[0] ==)
        printf("%s\n", *envp);
        envp++;
    }

    return 0;
}
